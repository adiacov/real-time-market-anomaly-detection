from market_anomaly.config import app_config, settings


def main() -> None:
    """Entry point for the market anomaly detection application."""
    print("Hello from real-time-market-anomaly-detection!")
    
    print("Testing app configs.")
    print(app_config)
    print(settings)
